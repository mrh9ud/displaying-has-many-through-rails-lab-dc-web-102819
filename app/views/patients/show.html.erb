<h1><%= @patient.name %></h1>
    <ul>
    <% @patient.appointments.each do |appt| %>
    <p>Doctor: <%= link_to appt.doctor.name, doctor_path(appt.doctor) %></p>
    <p>Appointment: <%= appt.appointment_datetime.strftime("%B %d, %Y at %H:%M") %></p>
    <% end %>
    </ul>